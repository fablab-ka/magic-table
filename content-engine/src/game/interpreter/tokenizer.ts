import { Statement, StatementInstance } from "../../types";
import { Token } from "./types";

export default class Tokenizer {
  public tokenizeMarkers(statements: StatementInstance[]): Token[][] {
    return [];
  }
}
